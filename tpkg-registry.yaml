schema_version: 1
# Registry template for the openjdk RUNTIME entry — rendered by the
# publish job (sed tokens from the built shards/sidecars) and landed on
# main by bot PR, replacing tools' `tebako publish --registry-out` flow
# (the CLI refuses kind: runtime — runtimes publish through the runtime
# release flow). kind: runtime entries carry NO entrypoints key (the
# registry validator: only apps and toolkits declare entrypoints).
payloads:
- name: openjdk
  kind: runtime
  versions:
  - version: '21.0.12'
    platforms:
      aarch64-macos:
        artifact: tebako-runtime-2.1.0-21.0.12-macos-arm64.tfs
        sha256: 'f548412d7ea3ca55ba92c401f7fb2c0c468341dade9ef9878dde0b9972a22882'
      x86_64-linux-gnu:
        artifact: tebako-runtime-2.1.0-21.0.12-linux-gnu-x86_64.tfs
        sha256: 'b37c6f2964b198e1311a8f4ff79ab43c43416c15685f64a0b1ccc10bb32c9c71'
      x86_64-windows-ucrt:
        artifact: tebako-runtime-2.1.0-21.0.12-windows-ucrt64.tfs
        sha256: 'b04ab5c3f615006eb3dc915c30ff3e08607e59a168bf17037617e3e3a50fb32d'
    release:
      ref: tfs:github:tebako-packages/openjdk:v2.1.0
  default: '21.0.12'
