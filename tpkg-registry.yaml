schema_version: 1
# Registry template for the openjdk RUNTIME entry — rendered by the
# publish job (sed tokens from the built shards/sidecars) and landed on
# main by bot PR, replacing tools' `tebako publish --registry-out` flow
# (the CLI refuses kind: runtime — runtimes publish through the runtime
# release flow). kind: runtime entries carry NO entrypoints key (the
# registry validator: only apps and toolkits declare entrypoints).
payloads:
- name: openjdk
  kind: runtime
  versions:
  - version: '21.0.12'
    platforms:
      aarch64-macos:
        artifact: tebako-runtime-2.1.3-21.0.12-macos-arm64.tfs
        sha256: '56cc8cf26963225b490167f1f6ec6cd8623e5604a323aedc7cb1d3f5564e9771'
      x86_64-linux-gnu:
        artifact: tebako-runtime-2.1.3-21.0.12-linux-gnu-x86_64.tfs
        sha256: 'c0a75d9d6bb466fb24271ddab59d67c8cff6ba46210f8822a8801128e12ac5a4'
      x86_64-windows-ucrt:
        artifact: tebako-runtime-2.1.3-21.0.12-windows-ucrt64.tfs
        sha256: 'c14b39be0115f720affa27dc522953f7ecaa7fc4b90754899e855c294471001a'
    release:
      ref: tfs:github:tebako-packages/openjdk:v2.1.3
  default: '21.0.12'
