schema_version: 1
# Registry template for the openjdk RUNTIME entry — rendered by the
# publish job (sed tokens from the built shards/sidecars) and landed on
# main by bot PR, replacing tools' `tebako publish --registry-out` flow
# (the CLI refuses kind: runtime — runtimes publish through the runtime
# release flow). kind: runtime entries carry NO entrypoints key (the
# registry validator: only apps and toolkits declare entrypoints).
payloads:
- name: openjdk
  kind: runtime
  versions:
  - version: '21.0.12'
    platforms:
      aarch64-macos:
        artifact: tebako-runtime-2.1.4-21.0.12-macos-arm64.tfs
        sha256: 'a9e16a51664dfaaac2c680a3d02b2c10602ac14001fb34dda4349d947ce6daf6'
      x86_64-linux-gnu:
        artifact: tebako-runtime-2.1.4-21.0.12-linux-gnu-x86_64.tfs
        sha256: '46299cae7125ce24febdc246f7c180b3bcb7ac1b8e19bfd855944045535e6440'
      x86_64-windows-ucrt:
        artifact: tebako-runtime-2.1.4-21.0.12-windows-ucrt64.tfs
        sha256: '9b1516af38ef1afdc06e8bf68e133c3141a22386237dd7388e312817ecf93c57'
    release:
      ref: tfs:github:tebako-packages/openjdk:v2.1.4
  default: '21.0.12'
